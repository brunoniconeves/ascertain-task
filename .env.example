## Application environment
# Used by scripts/seed_patients.py to decide whether to seed demo data.
APP_ENV=development

## Database
# For docker-compose local dev:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/ascertain

## Patient notes (local filesystem storage)
# Base directory where note files are stored (relative to project root in docker volume).
NOTES_BASE_DIR=./data/notes

# Max upload size for note files (bytes). Default is 5 MB.
NOTES_MAX_UPLOAD_BYTES=5242880

# Allowed MIME types for note uploads (pydantic will parse JSON arrays here).
NOTES_ALLOWED_MIME_TYPES=["text/plain","application/pdf","image/png","image/jpeg"]

# OpenAPI API KEY
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini   # optional