apiVersion: v1
kind: Secret
metadata:
  name: ascertain-secrets
  labels:
    app.kubernetes.io/name: ascertain-api
    app.kubernetes.io/part-of: ascertain-task
type: Opaque
stringData:
  # Example only. Do NOT commit real secrets.
  #
  # In real usage, create this Secret at deploy time (see k8s/README.md).
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@YOUR_POSTGRES_HOST:5432/ascertain"
  OPENAI_API_KEY: "replace-me"


